# Copy to .env and adjust before running `docker compose up`.
# Without a .env the defaults in docker-compose.yml are used, which are fine
# for local development but must NOT be reused on a public server.

DB_NAME=traffic
DB_USERNAME=traffic
DB_PASSWORD=traffic
