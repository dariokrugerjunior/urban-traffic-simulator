# Backend base URLs. Copy to `.env` (or set in your host's build env) to point the
# frontend at deployed services. Defaults target localhost for development.
VITE_TRAFFIC_API_URL=http://localhost:8081
VITE_ROUTING_API_URL=http://localhost:8082
